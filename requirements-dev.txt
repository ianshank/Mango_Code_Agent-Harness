# Dev/tooling dependencies. Runtime dependencies (fastapi/uvicorn/pydantic/httpx)
# live in requirements.txt, included below so `pip install -r requirements-dev.txt`
# keeps installing both, unchanged from before this split.
-r requirements.txt
# pytest 8.4.2 carries PYSEC-2026-1845; the fix release (9.0.3) requires
# Python >=3.10, so the pin forks on the interpreter and the universal lock
# carries both. The 3.9 leg keeps 8.4.2 and its dependency-audit job reports
# the advisory as a finding (that leg is continue-on-error); it goes away with
# the 3.9 floor (DEC-028).
pytest==9.0.3; python_version >= "3.10"
pytest==8.4.2; python_version < "3.10"
pytest-cov==7.1.0
pytest-mock==3.15.1
# Order randomisation (audit H8): `make test-python`/`coverage-python` pass
# `-p randomly`, so every run shuffles under a seed printed in the header and an
# order coupling reproduces with `--randomly-seed=N`. The pin forks on the
# interpreter like pytest's: 4.0 raised the floor to Python >=3.10, so the 3.9
# leg keeps 3.16.0 until the floor moves (DEC-028).
pytest-randomly==5.0.0; python_version >= "3.10"
pytest-randomly==3.16.0; python_version < "3.10"
# Parallel runs (audit H8): both Python runners pass `-n auto`. Enabled on the
# coverage run only after measuring that pytest-cov combines the workers' data
# and that INV-2's skip evidence stays complete under xdist (see the Makefile).
pytest-xdist==3.8.0
# Egress floor (R-EGF-1): sockets are disabled by default in addopts so an
# attempted connection fails the test instead of succeeding quietly.
pytest-socket==0.7.0
# The audit gate's scanner (audit M15). Declared here rather than installed by
# name in the workflow so it lands in the hashed lock and installs with
# `--require-hashes` like every other tool; the Makefile reads this pin back as
# PIP_AUDIT_VERSION. 2.9.0 is the newest release still declaring Python >=3.9.
pip-audit==2.9.0
# tomllib entered the stdlib in 3.11; tomli is its backport. The
# configuration-liveness gates read pyproject.toml, so without this they would
# fail to import on the 3.9 and 3.10 matrix legs.
tomli==2.4.1; python_version < "3.11"
ruff==0.16.5
mypy==1.11.2
# Dead-code gate in `make lint-python` (tech-debt-hardening-plan R-TDH-17);
# framework-registered symbols it cannot see are listed in vulture_whitelist.py.
vulture==2.16
# The lock tool. `make lock` compiles this file plus requirements-langgraph.txt
# into requirements-lock.txt with `uv pip compile --universal`, which keeps the
# environment markers a single pip-compile run would evaluate away, so one lock
# serves every interpreter in the CI matrix (tech-debt-hardening-plan R-TDH-9).
# Always invoked as `python -m uv`, never a bare binary on PATH (DEC-013).
uv==0.11.15
