{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PROJECT_DIR}/.mango/hooks/block_dangerous.sh\""
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PROJECT_DIR}/.mango/hooks/loop_detection.sh\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PROJECT_DIR}/.mango/hooks/pre_completion_checklist.sh\""
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PROJECT_DIR}/.mango/hooks/save_state_before_compact.sh\""
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PROJECT_DIR}/.mango/hooks/session_start.sh\""
          }
        ]
      }
    ]
  },
  "$comment": "Mango agent lifecycle hook bindings. DORMANT BY DECISION DEC-003: these are declared here but deliberately NOT mirrored into .claude/settings.json, which is the file Claude Code reads, so none of them run. Commands are routed through `bash` (matching .claude/settings.json) because every tracked .sh in this repository is mode 644 by convention -- invoking the bare path would fail with Permission denied the moment these were woken. test_mango_hooks_stay_dormant pins the dormancy; test_hook_references_only_paths_that_exist pins that the scripts stay correct while dormant."
}
