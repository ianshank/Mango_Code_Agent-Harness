# Runtime dependencies for harness/api_server. Split out from requirements-dev.txt
# so `pip install -e .` (which reads [project.dependencies] in pyproject.toml,
# mirrored here) declares its own real dependencies instead of relying on a
# "dev" file for anything the running service actually needs.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.13.5,<3.0
httpx>=0.27,<1.0
mcp>=2.0.0,<3.0; python_version >= "3.10"
