# The LangGraph StateGraph runtime, installed on every CI leg now that the
# floor is Python >=3.10 (docs/specs/python-floor-310.md; langgraph itself
# declares Requires-Python >=3.10, so the version marker this pin carried
# before the floor moved would be a permanently-true tautology). Scanned by
# `make audit-python` alongside requirements.txt. Mirrors pyproject.toml's
# `langgraph` extra (single set of literals; test_dependency_lockstep.py keeps
# both in step). The Postgres checkpointer is deliberately not here: nothing
# under harness/ imports it.
langgraph>=1.0.10,<2.0
