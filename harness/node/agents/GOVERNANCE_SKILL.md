# Governance Skill

Reviewed: 2026-08-24

## Purpose

Apply the Agentic SSD gate contract, preserve the external root of trust, refuse governance weakening as a means to pass a gate, and require side-effect evidence for governed agent actions.

## Decisions since 2026-08-24

- DEC-000 — Projections are explicitly not applicable in the uninstantiated template; adopters must configure mappings or record a replacement decision.
- DEC-001 — Live smoke tests against remote NVIDIA NIM API are conditionally skipped when endpoints are rate-limited or unavailable.
- DEC-002 — `protected_paths` covers the agent control surface and carries root-relative twins for the multi-stack layout; more changes now require the `infra-reviewed` attestation, by design.
- DEC-003 — The unbound `.mango/hooks/` scripts remain dormant; only the `.claude/` SessionStart hook is live.
- DEC-004 — Unwired policy keys are classified with reviewed reasons instead of deleted; duplicated grammar/limits/tool-pin values are pinned by cross-check tests; coverage is enforced as separate lines and branches floors; the bundle's top-level digests are regenerated inside `digest-regen`.
- DEC-005 — The MAS orchestrator consults the PreToolUse guard in-process from the installed harness, never from `workspace_dir`, and guard unavailability denies. Agent-initiated `git push` is blocked as an accepted consequence: the repository root carries no remote allowlist and the guard fails closed.
- DEC-006 — The guard canonicalises its payload envelope across `tool_input` and `args` and denies a JSON object carrying neither; non-JSON input keeps its existing leg.
- DEC-007 — `protected_paths` is enforced at tool-call time by the write gate, not only by CI; `.git/**` is denied explicitly because git never lists it; the PDP, the write gate and the orchestrator become protected paths.
- DEC-008 — Role tool exposure is derived from `agent-policy.json` (union of canonical contracts minus approval-gated actions), so the verifier no longer holds `write_file`; hook environments are stripped of credentials.
- DEC-009 — The policy decision point runs in process; the reference PDP is retained as the external contract and pinned by an agreement test. The `exists()` fail-open is removed.
- DEC-010 — A command's action is derived from the command and fails closed to an action no role holds; the broker's process backend contains cwd, runtime and output size, and explicitly does not isolate.
- DEC-011 — `run_command` routes through `ExecutionBroker`, so INV-8 is enforced on the live path. Active roles execute as the narrowest canonical contract; `pip install` and other external actions are denied for the reasoner. `test_invariant_liveness.py` ships with no waivers.
- DEC-012 — `read_file` and `apply_patch` join the tool surface behind a new `read_policy.py`, the read-side counterpart to `write_policy.py`; both compose one shared credential-filename pattern with `command_actions.classify` so the shell-command door and the direct-file door cannot drift apart. `apply_patch` reuses `write_denial_reason` unchanged and grades as the same `write` action as `write_file`, so the verifier still holds no write-shaped tool.
- DEC-013 — A local bare `ruff` resolving to 0.15.8 (vs. the pinned 0.6.9 that `python -m ruff`/`make lint`/CI actually use) produced 3 false ruff findings during research; retracted, `main` was already clean under the pinned version, and `harness/__init__.py`/`harness/api_server/__init__.py` (added for namespace-package consistency, briefly reverted on the same false signal) are restored. Wiring `lint-node` into `ci` was tried and reverted for a separate, genuine reason: `make lint-node` currently crashes on a pre-existing `typescript`/`typescript-eslint` version incompatibility unrelated to this batch, so `ci`'s prerequisites stay untouched and this is a tracked follow-up instead. A root `audit` target (pip-audit + delegated Node osv-scanner) closes the `KNOWN_GAPS["audit"]` exception via a dedicated workflow job, mirroring `secrets`. `requirements.txt` splits runtime deps out of `requirements-dev.txt` and joins `protected_paths`. `governance/broker.py` and `check_dedup.py` share one non-raising JSON-parse classifier (`governance_json.py`); `policy_loader.py`, `coverage_gate.py`, and `validate_invariants.py` stay excluded, per `policy-single-source.md`'s standalone-stdlib decision and their own individually-pinned exception messages. A root `.governance/` was again considered and rejected, per DEC-005.

- DEC-014 — `make secrets`'s `gitleaks git` invocation (all three Makefiles) scanned every ref in the local clone, not just the checked-out branch; an unrelated concurrent branch's leaked key was failing PR #35's `secret-scan` even though its diff never touched that branch. `--log-opts="HEAD"` now scopes the scan to the current ref's own ancestry, matching INV-1's intent that a PR's gate be actionable by that PR's author.
- DEC-015 — GitHub Copilot's review of PR #35 found six real defects in the DEC-013/DEC-014 batch, all fixed here: `governance_json.py` now classifies a `UnicodeDecodeError` as `malformed` instead of letting it escape uncaught; `broker.py`'s `_load_json` again raises `FileNotFoundError`/`OSError`/`ValueError` per R-DH-5 instead of one collapsed `ValueError`, with new direct tests; `secrets` joins `pre-pr`'s prerequisites, matching README's claim that `pre-pr` runs both scanners; `pip-audit` is pinned (`PIP_AUDIT_VERSION`) like gitleaks/osv-scanner; a new `audit-matrix` CI job runs `audit-python` across 3.9/3.10/3.12 so a vulnerability specific to another supported interpreter cannot be missed; a new liveness test asserts nothing can guard the audit job behind an `if:`. Also fixed: DEC-013's summary here self-contradicted on whether the two `__init__.py` files ship — the stale sentence is removed.
- DEC-016 — DEC-015's `PIP_AUDIT_VERSION` pin (2.10.1) broke the new `audit-matrix` job's 3.9 leg immediately: `pip-audit` 2.10.0 raised its own floor to `Requires-Python >=3.10`, so 3.9 had no installable candidate. The 3.10/3.12 legs passed clean on the same commit, confirming the matrix design itself was sound. Capped at 2.9.0, the newest release still declaring `Requires-Python >=3.9`.
- DEC-017 — `audit-matrix`'s first real run found 7 known CVEs across `starlette`/`click`/`python-dotenv`, all resolved only under Python 3.9; every currently-published fix for each requires Python >=3.10, so no `requirements.txt` pin can make that leg both installable and clean under 3.9. The 3.9 leg alone is `continue-on-error` (finding stays visible in its output, never silenced); 3.10 and 3.12 stay fully enforced. Sharpens the already-deferred Python 3.9 floor bump from an EOL concern into an active, unpatchable-CVE one.
- DEC-018 — `NEXT_STEPS.md`'s required-status-check list (for the still-unapplied branch ruleset) was accurate when written but never updated after DEC-013/DEC-016/DEC-017 added `dependency-audit`/`dependency-audit (3.x)`; a ruleset built from the stale 5-check list would leave that entire gate, CVE legs included, permanently unrequired. Corrected to the 9 names `python-package.yml` actually reports; `test_ci_gate_coverage.py` now parses both the workflow and the doc and fails closed on divergence, proved by reverting both a renamed-job case and a doc-omits-a-name case. Applying the ruleset itself remains a manual repository-settings step.
- DEC-019 — The triplicated `digest()` helper across the three control-plane scripts is confirmed intentional (each script deliberately imports zero `harness.shared` symbols, per the root-of-trust design that control-plane verifiers must not trust the governed project's own copy of anything) and left as-is rather than deduplicated.
- DEC-020 — New gate-like modules land under `harness/shared/gates/` going forward; the 11 existing `check_*.py`/`validate_*.py` files stay where they are, since migrating them touches ~15 `protected_paths` entries and `check_dedup.py`'s shim mapping for no behavior change.
- DEC-021 — A coverage-regression check was implemented via `scheduled-drift.yml`'s nightly `main-drift` loop (`coverage-python` added alongside `digest-regen`/`check-dedup`/`validate`), not by wiring the dormant Stop hook `.mango/hooks/pre_completion_checklist.sh` — `.claude/settings.json` doesn't read `.mango/settings.json` (DEC-003), so that would look wired while doing nothing. Justified by `main` having no branch ruleset yet: per-PR CI is advisory, so a coverage-dropping PR could merge unnoticed until this catches it nightly.
- DEC-022 — Two second-round audit findings evaluated individually rather than mechanically fixed: `verification.py`'s `timeout: int = 300` duplicates a policy value but the module documents a deliberate zero-filesystem-read-at-import contract that sourcing it from policy would violate, and the one production caller already injects the real value — left as-is. `langgraph/decorators.py`'s `@with_authority`/`@budgeted` are implemented and tested in isolation but never applied to any real node function, contradicting a checked-off `NEXT_STEPS.md` claim (now corrected); both also fail open, moot only while unwired. Wiring them is deferred to its own spec, since it requires fixing the fail-open behavior in the same change, not just adding decorator syntax.
- DEC-023 — PR #55's unconditional `mcp>=1.0.0,<3.0` in `requirements.txt` broke CI two ways: Python 3.9 (no published `mcp` release supports below 3.10) and mypy (`mcp_server.py` passed `inputSchema`, but `Tool`'s field is `input_schema` from 2.0.0 onward — 1.x has no such field or alias, so the true fix is a version floor, not just a rename). Fixed by tightening to `mcp>=2.0.0,<3.0; python_version >= "3.10"` and renaming the call site. Moving `mcp` into `[project.optional-dependencies]` alongside `langgraph` was considered and rejected for now: architecturally consistent, but no CI job installs any extras group today, so it would permanently remove mypy's ability to catch this exact bug class. `test_dependency_lockstep.py` is added: `requirements.txt` and `pyproject.toml`'s `[project.dependencies]` had no mechanical check that they declare the same packages, which is exactly how this landed in one file and not the other, missed across four rounds of human review.
- DEC-024 — PR #60 merged with every CI run on its head red under a commit message claiming `make ci` and mypy clean; `main` had no ruleset to stop it. A ruleset export requiring the nine derived status checks plus code-owner review is committed at `.github/rulesets/main.json` (pinned to the workflow by `test_workflow_contracts.py`; the owner applies it by import), and `CLAUDE.md`/`CONTRIBUTING.md`/the PR template now state that a verification claim in prose is not evidence — only the check runs on the pushed head are. A PR-body-emptiness CI job was considered and rejected as satisfiable by pasting anything.
- DEC-025 — Constant triage: every operational constant the audit found unlinked is now a policy value (`process_backend.DEFAULT_MAX_OUTPUT_BYTES` → `orchestrator.max_output_bytes`; `retry_policy.DEFAULT_MAX_RETRIES` equality-pinned to `nemotron.max_retries`) or accepted with a reason (retry backoff shape, the shadow-planner env knob, the cognitive-signal protocol ceilings, the Node client's constructor-overridable resilience defaults). `test_constant_triage.py` holds the inventory and fails on any row with neither a policy key nor a `DEC-` id.
- DEC-026 — Python skip accounting: `conftest.py` writes every pytest skip to a TSV that `make verify-zero-skips-python` (in `ci` and `ci-python`) feeds to the existing `verify_zero_skips.py` gate; four empty-parametrize skips became loops; the gate gains `unique_id_glob` for JUnit waivers (decision id still required in the skip reason); waivers live in `harness/shared/tests/skip-waivers.json` because the root `.governance/` stays dormant (DEC-005).
- DEC-027 — `autonomous_healing.py` and `lats_optimizer.py` parked under `harness/shared/experimental/` (unchanged, tested, policy-sourced; never reachable from a runtime path; INV-15 keeps LATS off). Old paths are deprecation shims for one minor release. The orchestrator facade loses its three test-only pass-throughs; `execute_sequential_thinking_loop` stays (R-ORCH-4, R-VP-11).
- DEC-028 — The Python 3.9 floor stays; its two costs are declared, not hidden. `coverage.optional_extras` names each optional extra (`import_name`, `deselect_env`, `path_prefixes`); `coverage_gate.py` waives the per-file floor for those paths only on a leg that sets the env AND cannot import the extra, logging each waived file, with aggregate floors and every other file still enforced; `conftest.py` reads the deselect variable's name from the same key. `pytest` forks on the interpreter (`9.0.3` on ≥3.10 for PYSEC-2026-1845, `8.4.2` below) and the 3.9 dependency-audit leg keeps reporting the advisory (continue-on-error, pre-existing). Both end with the 3.9 floor.
- DEC-029 — DEC-020 stands: `harness/shared/` is not regrouped (cyclic on the real import graph; `protected_paths`, `check_dedup.py` and the per-stack delegators name the flat paths); new gate-like modules go under `harness/shared/gates/`. The 31 `sys.path` bootstrap sites in four styles are accepted as-is: a helper would need the bootstrap it replaces, the per-stack scripts are digested root-of-trust artefacts (DEC-004), and `check_dedup.py` already bounds each shim. Revisit with the entrypoint contract when the 3.9 floor moves (DEC-028).
- DEC-030 — The Python skip-evidence hooks (DEC-026) and the langgraph deselection live in the repository-root `conftest.py` (logic in `harness/shared/tests/_session_hooks.py`). Registered in `harness/shared/tests/conftest.py`, pytest scoped them to that directory, so skips under `harness/api_server/tests` were never recorded; with `harness/control-plane/tests/` colocated (R-TDH-26) the gap would have widened. `test_session_hooks.py` runs the real root conftest over two sibling suites and pins that no directory conftest re-registers the hooks.
- DEC-032 — Post-implementation review found four gates that could report PASS on absent or wrong evidence, all fixed: the Python zero-skip hook ignored collection-time skips (a `CollectReport`, not a `TestReport`) so a whole skipped module left no row; `coverage_gate._waiving_extra` used a raw `startswith` so a prefix missing its trailing slash waived every sibling; `check_per_file` printed `[PASS] … 0 file(s)` when the waiver set covered everything; and the constant-triage decision check reduced to matching the string `harness`, so a constant could cite a decision about a different module. Also fixed a reproducible order-dependent failure (a `runpy` gate run permanently reconfigured the global `__main__` logger), an unguarded extras index that killed collection with a bare `KeyError`, and the untested `deselect_langgraph`. Corrects DEC-027: `lats_optimizer.py` is byte-identical after the move, `autonomous_healing.py` is not (it carries the Phase 2 `BROKER_SUCCESS` substitution).
- DEC-031 — Dependabot PRs #38–#46 closed as superseded by the universal lock and the Phase 1 toolchain bump (every proposed version is carried at or above); #40 additionally not adopted because a fastapi floor of >=0.141.1 breaks the 3.9 leg. The weekly `lock-upgrade-check` job is the Python-side upgrade signal from here on.

## Required behavior

1. Read `.governance/policy.json`, `.governance/agent-policy.json`, and the decision log before changing governed controls.
2. Do not grant a child agent authority its declared role does not independently possess.
3. Route external writes and production changes through the independent Tool Broker / PDP and required human approval.
4. Run the named governance gates and preserve their evidence; never convert missing security tooling into a pass.

- DEC-033 — The `pip` ecosystem is removed from `.github/dependabot.yml`, completing DEC-031 instead of restating it. DEC-031 named `lock-upgrade-check` the Python upgrade signal but left the ecosystem enabled, so twelve bot PRs reopened on 2026-09-02 (#62–#73) including an unrequested `mypy` 1.11 → 2.3 major. Against a locked dependency set a per-package pin is a no-op or a reintroduction of what the lock supersedes. `npm` and `github-actions` stay (neither is covered by the Python lock; R-TDH-10 wants the action majors moving). Re-enabling requires superseding DEC-031, not editing the config.
- DEC-034 — `make lint-node` is wired into `make ci`, and the recorded blocker was not the real one. DEC-013 blamed a `typescript`/`typescript-eslint` incompatibility; measured with the workspace installed, ESLint and Knip pass and only `prettier --check` fails, on the digest-pinned `harness/node/.governance/policy.json`. Prettier's formatting and the root-of-trust SHA256 are mutually exclusive, so `harness/node/.prettierignore` excludes the `.governance/` tree — the only change that lets both gates hold. `lint-node` is a prerequisite of `ci` and deliberately not of `ci-python` or `lint`, whose matrix legs install no pnpm; this puts R-TDH-23's policy-sourced ESLint `max-lines` rule into a CI job for the first time.
- DEC-035 — `coverage_gate.py` (470/500 lines) splits into threshold enforcement and `coverage_scope.py` (which files the floors judge), along the seam every defect in this area has drawn; backwards compatible via re-exports, both modules at 100%. The split invalidated three test-side assumptions no assertion caught — a `__file__` patch that made the DEC-032 shadow regression assert nothing, and sixteen `caplog` captures anchored on one module's logger, now on the `harness.shared` parent. Also fixes two defects in the batch's own gates: `declared_source_roots` matched nothing when `[tool.coverage.run]` was the file's last table, and a `# keep:` comment anywhere in `.gitleaks.toml` granted an allowlist exemption (now scoped to the `[allowlist]` block).
- DEC-036 — `nemotron.top_p` becomes a policy key read by both stacks. The Node client hard-coded `0.7` in two duplicated request-body literals while the Python bridge omitted `top_p` entirely, so the stacks sampled differently against the same endpoint; the parity test could not see it because it asserts the request succeeds rather than comparing bodies. Adding the key touched `policy_loader` (exact-equality pinned), the Node `NemotronPolicy`, and `policy-artifact.json` (rebuilt via `publish_policy_artifact.py build`; the per-stack mirrors carry no `nemotron` block). The duplicated literals collapse into `buildChatRequestBody`, which takes the policy as an argument so a test can prove the body follows it. `SAMPLING_BOUNDS` stays out of policy: those are the provider's input domain, not a tunable.
- DEC-037 — `retry.ts`'s `JITTER_CEILING_MS` is a true constant, recorded separately because DEC-025 names neither it nor `retry.ts`, so a row citing DEC-025 fails the linkage check. Python jitters proportionally (a dimensionless ratio of the delay), Node adds an absolute millisecond ceiling — not the same knob in two languages, nothing to drift from; the governance threshold here is `max_retries`, already policy-sourced. Landed with three rows DEC-025 had justified but the inventory never registered (`resetTimeoutMs`, `halfOpenSuccessThreshold`, `maxBackoffMs`).
- DEC-038 — The protected-path attestation table is derived and verified, not transcribed. `harness/shared/governance/attestation.py` imports `is_protected`, `git_modified_files` and `load_protected_patterns` from `validate_invariants` (asserted as symbol identity, so the printed table and the enforced set cannot diverge), prints the table for a change, and `--check` compares a PR description against it — failing closed on a missing attestation section, a section with no table, or a mismatch in either direction. Written because a comment on PR #76 claimed thirteen attested rows where the validator's set was ten: an inflated count reads as thoroughness, which is the DEC-024 class inside the control meant to prevent it. Scoped to the section under a configurable heading; base branch resolved from the flag, `GITHUB_BASE_REF`, then the remote's `origin/HEAD`, never a literal. Wired as `make attestation` / `make attestation-check` and a `build-full` step that runs on every pull request and is not gated on the `infra-reviewed` label.
