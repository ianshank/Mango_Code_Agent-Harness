# Governance Decision Log

Format: `YYYY-MM-DD | ID | decision | owner`

2026-08-24 | DEC-000 | Template has no generated projections until an adopter configures mappings | template-maintainers
2026-08-26 | DEC-001 | Live smoke tests against remote NVIDIA NIM API are conditionally skipped when endpoints are rate-limited or unavailable | ai-maintainers
2026-08-27 | DEC-002 | protected_paths gains the agent control surface (CLAUDE.md, .claude/ and .mango/ hooks, skills, agent-policy, CONTRACT, pyproject, the policy publisher) and root-relative twins for the multi-stack layout; ~32% of historical commits would newly require the infra-reviewed label, accepted as the cost of gating what agents may change about their own permissions | governance-maintainers
2026-08-27 | DEC-003 | The five unbound .mango/hooks/ scripts stay dormant: .mango/settings.json is not the file Claude Code reads, so waking them would change tool-call behavior for every session on logic that has never executed; only the .claude/ SessionStart hook is live | governance-maintainers
2026-08-28 | DEC-004 | Unwired governance-policy keys are classified (DECLARED_NOT_YET_ENFORCED in test_policy_consistency.py) rather than deleted -- per-stack mirrors sit under root-of-trust and bundle digests, so removal is a rotation-sized change; the duplicated decision-ID grammar, agent_defaults limits, and gitleaks pin become cross-checked equalities; the stale unread harness/SHA256SUMS.txt manifest is deleted; Python coverage is enforced as two separate floors (lines and branches) by coverage_gate.py; build_policy_bundle.py joins digest-regen as the only regenerator of the bundle's top-level policy digests | governance-maintainers
2026-08-28 | DEC-005 | The MAS orchestrator consults the PreToolUse guard in-process from the installed harness rather than resolving harness/shared/pretooluse_guard.py under workspace_dir: the workspace is agent-writable, so a guard loaded from it can be replaced by the model whose commands it checks, and the previous `if guard_script.exists()` made the guard optional. Guard unavailability now denies (INV-9). Accepted consequence: agent-initiated `git push` is blocked, because the repository root carries no .governance/allowed-remotes.txt and remotes.py fails closed on the missing file; creating a root allowlist is rejected because `.governance/**` is a declared-dormant protected_paths pattern | governance-maintainers
2026-08-28 | DEC-006 | The guard canonicalises its payload envelope across `tool_input` and `args`, and denies a JSON object carrying neither with exit 2. The orchestrator sent `args.command` while the guard read `tool_input.command`, so every command it submitted was evaluated as the empty string and allowed. Accepting both shapes converts the historical payload from a silent allow into an evaluated command for any adopter still emitting it; non-JSON input keeps its existing leg, because denying every unmodelled payload shape would block tool calls this guard does not model | governance-maintainers
2026-08-28 | DEC-007 | protected_paths becomes a runtime write gate as well as a CI review gate: _execute_write_file consults validate_invariants.is_protected at tool-call granularity, reusing the CI matcher rather than a second implementation, and denies the git directory explicitly because git never lists it and protected_paths structurally cannot cover it. Enforcement was not absent before, it was at agent-turn granularity via the pre-nemotron-run hook, which leaves a whole tool-call budget unchecked and never re-validates the last agent's own writes. tool_broker_reference.py (the PDP the broker executes as a host subprocess before the guard runs), write_policy.py and mango_mas_orchestrator.py join protected_paths and CRITICAL_PATTERNS: the enforcement points were themselves unprotected | governance-maintainers
2026-08-28 | DEC-008 | Tool exposure is derived per active role from agent-policy.json rather than defaulting to the full schema. The union of a role's canonical contracts minus each contract's human_approval_required_for is the effective grant, so release-auditor's approval-gated external_write and production_change do not leak into the verifier. Consequence: the verifier no longer receives write_file, which every canonical contract it maps to already denied in prose. The hook environment is filtered of credential-named variables, enforcing agent-policy.json's declared secrets_may_not_be_propagated_to_subagents | governance-maintainers
2026-08-28 | DEC-009 | The policy decision point is evaluated in process rather than as a host subprocess. ExecutionBroker ran harness/control-plane/tool_broker_reference.py with sys.executable, on the host, before check_command -- from a path inside the agent's workspace, so replacing that file replaced every subsequent verdict; and the call was guarded by `if _PDP_PATH.exists() and _POLICY_PATH.exists()`, so a missing file skipped the verdict instead of denying. The decision is set membership; running it as a subprocess bought nothing and cost a code-execution primitive. tool_broker_reference.py is retained as the contract an external broker mirrors, and test_policy_decision.py pins that the two agree on every representative request | governance-maintainers
2026-08-28 | DEC-010 | The action a command exercises is derived from the command, never supplied by the caller: a constant action grades pytest and `rm -rf /` identically and human_approval_required_for is then never reached. Classification is an allowlist whose default, for anything unmodelled, is an action no role holds, so an unrecognised command denies for every agent deterministically. ExecutionBroker gains a process backend that pins the working directory, bounds runtime and caps captured output -- containment, explicitly not isolation: it confines neither the filesystem nor the network, so INV-13's sandbox digest remains unsatisfiable and no result claims otherwise. Isolation is deferred to a capability profile because the primitive cannot be exercised on this repository's CI runners, and a gate that cannot run is the defect this programme exists to close | governance-maintainers
2026-08-28 | DEC-011 | The MAS orchestrator routes run_command through ExecutionBroker, making INV-8 true on the live path for the first time: generated code now executes through the approved broker, which derives the action, obtains a policy verdict, runs the command guard, and pins cwd, timeout and captured output size. Active roles execute as the narrowest canonical contract that covers their work (reasoner as implementer, verifier as test-eval) rather than being added to agent-policy.json, which would be the agent's own governing policy gaining an execution grant; test_execution_identity_is_no_wider_than_the_role pins that the chosen identity is never wider than the role's derived authority. Accepted consequences: an active role the mapping does not declare cannot execute at all, and commands classified as external_write or destructive -- notably `pip install` -- are denied for the reasoner, so the reasoner prompt no longer instructs installing packages and directs the agent to knowledge_gap_log instead. test_invariant_liveness.py ships with an empty waiver dict; it was deliberately not landed alongside a waiver for INV-8, because a self-destructing waiver introduced in the same commit as its own fix can never fire | governance-maintainers
