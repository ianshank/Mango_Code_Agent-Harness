# Governance Decision Log

Format: `YYYY-MM-DD | ID | decision | owner`

2026-08-24 | DEC-000 | Template has no generated projections until an adopter configures mappings | template-maintainers
2026-08-26 | DEC-001 | Live smoke tests against remote NVIDIA NIM API are conditionally skipped when endpoints are rate-limited or unavailable | ai-maintainers
2026-08-27 | DEC-002 | protected_paths gains the agent control surface (CLAUDE.md, .claude/ and .mango/ hooks, skills, agent-policy, CONTRACT, pyproject, the policy publisher) and root-relative twins for the multi-stack layout; ~32% of historical commits would newly require the infra-reviewed label, accepted as the cost of gating what agents may change about their own permissions | governance-maintainers
2026-08-27 | DEC-003 | The five unbound .mango/hooks/ scripts stay dormant: .mango/settings.json is not the file Claude Code reads, so waking them would change tool-call behavior for every session on logic that has never executed; only the .claude/ SessionStart hook is live | governance-maintainers
2026-08-28 | DEC-004 | Unwired governance-policy keys are classified (DECLARED_NOT_YET_ENFORCED in test_policy_consistency.py) rather than deleted -- per-stack mirrors sit under root-of-trust and bundle digests, so removal is a rotation-sized change; the duplicated decision-ID grammar, agent_defaults limits, and gitleaks pin become cross-checked equalities; the stale unread harness/SHA256SUMS.txt manifest is deleted; Python coverage is enforced as two separate floors (lines and branches) by coverage_gate.py; build_policy_bundle.py joins digest-regen as the only regenerator of the bundle's top-level policy digests | governance-maintainers
2026-08-28 | DEC-005 | The MAS orchestrator consults the PreToolUse guard in-process from the installed harness rather than resolving harness/shared/pretooluse_guard.py under workspace_dir: the workspace is agent-writable, so a guard loaded from it can be replaced by the model whose commands it checks, and the previous `if guard_script.exists()` made the guard optional. Guard unavailability now denies (INV-9). Accepted consequence: agent-initiated `git push` is blocked, because the repository root carries no .governance/allowed-remotes.txt and remotes.py fails closed on the missing file; creating a root allowlist is rejected because `.governance/**` is a declared-dormant protected_paths pattern | governance-maintainers
2026-08-28 | DEC-006 | The guard canonicalises its payload envelope across `tool_input` and `args`, and denies a JSON object carrying neither with exit 2. The orchestrator sent `args.command` while the guard read `tool_input.command`, so every command it submitted was evaluated as the empty string and allowed. Accepting both shapes converts the historical payload from a silent allow into an evaluated command for any adopter still emitting it; non-JSON input keeps its existing leg, because denying every unmodelled payload shape would block tool calls this guard does not model | governance-maintainers
2026-08-28 | DEC-007 | protected_paths becomes a runtime write gate as well as a CI review gate: _execute_write_file consults validate_invariants.is_protected at tool-call granularity, reusing the CI matcher rather than a second implementation, and denies the git directory explicitly because git never lists it and protected_paths structurally cannot cover it. Enforcement was not absent before, it was at agent-turn granularity via the pre-nemotron-run hook, which leaves a whole tool-call budget unchecked and never re-validates the last agent's own writes. tool_broker_reference.py (the PDP the broker executes as a host subprocess before the guard runs), write_policy.py and mango_mas_orchestrator.py join protected_paths and CRITICAL_PATTERNS: the enforcement points were themselves unprotected | governance-maintainers
2026-08-28 | DEC-008 | Tool exposure is derived per active role from agent-policy.json rather than defaulting to the full schema. The union of a role's canonical contracts minus each contract's human_approval_required_for is the effective grant, so release-auditor's approval-gated external_write and production_change do not leak into the verifier. Consequence: the verifier no longer receives write_file, which every canonical contract it maps to already denied in prose. The hook environment is filtered of credential-named variables, enforcing agent-policy.json's declared secrets_may_not_be_propagated_to_subagents | governance-maintainers
2026-08-28 | DEC-009 | The policy decision point is evaluated in process rather than as a host subprocess. ExecutionBroker ran harness/control-plane/tool_broker_reference.py with sys.executable, on the host, before check_command -- from a path inside the agent's workspace, so replacing that file replaced every subsequent verdict; and the call was guarded by `if _PDP_PATH.exists() and _POLICY_PATH.exists()`, so a missing file skipped the verdict instead of denying. The decision is set membership; running it as a subprocess bought nothing and cost a code-execution primitive. tool_broker_reference.py is retained as the contract an external broker mirrors, and test_policy_decision.py pins that the two agree on every representative request | governance-maintainers
2026-08-28 | DEC-010 | The action a command exercises is derived from the command, never supplied by the caller: a constant action grades pytest and `rm -rf /` identically and human_approval_required_for is then never reached. Classification is an allowlist whose default, for anything unmodelled, is an action no role holds, so an unrecognised command denies for every agent deterministically. ExecutionBroker gains a process backend that pins the working directory, bounds runtime and caps captured output -- containment, explicitly not isolation: it confines neither the filesystem nor the network, so INV-13's sandbox digest remains unsatisfiable and no result claims otherwise. Isolation is deferred to a capability profile because the primitive cannot be exercised on this repository's CI runners, and a gate that cannot run is the defect this programme exists to close | governance-maintainers
2026-08-28 | DEC-011 | The MAS orchestrator routes run_command through ExecutionBroker, making INV-8 true on the live path for the first time: generated code now executes through the approved broker, which derives the action, obtains a policy verdict, runs the command guard, and pins cwd, timeout and captured output size. Active roles execute as the narrowest canonical contract that covers their work (reasoner as implementer, verifier as test-eval) rather than being added to agent-policy.json, which would be the agent's own governing policy gaining an execution grant; test_execution_identity_is_no_wider_than_the_role pins that the chosen identity is never wider than the role's derived authority. Accepted consequences: an active role the mapping does not declare cannot execute at all, and commands classified as external_write or destructive -- notably `pip install` -- are denied for the reasoner, so the reasoner prompt no longer instructs installing packages and directs the agent to knowledge_gap_log instead. test_invariant_liveness.py ships with an empty waiver dict; it was deliberately not landed alongside a waiver for INV-8, because a self-destructing waiver introduced in the same commit as its own fix can never fire | governance-maintainers
2026-08-30 | DEC-012 | read_file and apply_patch join the tool surface, alongside the read-side policy that had to exist first. read_policy.py is the counterpart to write_policy.py: without it, a read_file mapped to the `read` action would have bypassed command_actions.classify entirely -- `cat .env` already denies as secret_access, an action no role holds, but a direct filesystem read graded plain `read` would have returned NVIDIA_API_KEY into conversation_history, sent back to the model API on the next turn. command_actions.py now composes its credential-filename regex from read_policy.CREDENTIAL_FILENAME_ALTERNATION rather than restating it, so the shell-command door and the direct-read door share one definition instead of two that can drift; a parity regression test asserts this as a property over a corpus rather than a fixed list. apply_patch reuses write_denial_reason unchanged, inheriting write_file's protected-path and .git-segment denial rather than a second implementation; agent-policy.json is unchanged, and apply_patch grades as the same write action as write_file, so the verifier -- which holds no write -- receives read_file and not apply_patch (R-AC-8 preserved under the new tool's name). Accepted consequence: read_file and apply_patch use explicit open(..., newline="") rather than Path.read_text/write_text, because the latter silently translates CRLF to LF on read and does not restore it on write, turning a one-word patch into a whole-file diff -- a defect caught only by testing a CRLF fixture byte-for-byte, not by any existing gate | governance-maintainers
2026-08-30 | DEC-013 | Tech-debt/CI-enforcement batch (docs/specs/ci-enforcement-gaps.md, docs/specs/dependency-hygiene.md). Retracted mid-PR: an initial research pass claimed 3 live ruff findings (2 unused `noqa: E402` in harness/api_server/tests/test_main.py, 1 missing `noqa: BLE001` in harness/shared/write_policy.py) and "fixed" them; CI then failed `make ci-python`'s `lint-python` step on those exact two files with the OPPOSITE verdict (E402 live, the added BLE001 noqa unused). Root cause: this environment has a bare `ruff` on PATH resolving to 0.15.8, while `python -m ruff` -- what the Makefile, `make lint`, and CI's `pip install -r requirements-dev.txt` all actually resolve to -- is the pinned 0.6.9, and the two versions disagree on both rules for this exact code shape. Every local verification in this batch that used the bare `ruff`/`mypy` binary instead of `python -m ruff`/`python -m mypy` or a `make` target is unreliable; `main` was already clean under the pinned 0.6.9, so both files are reverted to their pre-PR state and no ruff fix ships in this batch. The same misdiagnosis initially caused harness/**init**.py and harness/api_server/**init**.py (added for namespace-package consistency: both packages are genuinely imported as harness.x.y, matching harness/shared/**init**.py's existing convention) to be added, then reverted after the bare-ruff run reported an unrelated lint-rule interaction; re-tested under the correctly-pinned `python -m ruff`, the two files produce zero findings, so they are restored and ship in this batch after all. `make lint-node` genuinely does crash (`typescript-eslint does not support TS 7.0`, a pre-existing `typescript` 7.0.2 / `typescript-eslint` 8.67.0 incompatibility in harness/node/package.json) -- an independent, Node-toolchain-side finding unaffected by the Python tool-version issue -- so wiring it into `ci` remains out of this batch, tracked as a follow-up in docs/specs/ci-enforcement-gaps.md's Open questions. A root `make audit` (pip-audit against a new requirements.txt, delegated to the Node stack's existing osv-scanner target) closes the `KNOWN_GAPS["audit"]` exception in test_ci_gate_coverage.py via a dedicated workflow job, mirroring how `secrets` is enforced outside `ci` rather than duplicated across matrix legs. requirements.txt splits the API server's runtime dependencies (fastapi, uvicorn, pydantic, httpx) out of requirements-dev.txt, which keeps installing both via a `-r requirements.txt` include, and joins protected_paths alongside it; pyproject.toml gains a mirrored `[project.dependencies]`. governance/broker.py's and check_dedup.py's JSON-parsing consolidates into harness/shared/governance_json.py, a non-raising classifier -- each caller still raises its own existing exception type and log message from the classification, so check_dedup.py's SystemExit(1)/"Malformed governance policy" pin and broker.py's broadly-caught ValueError are both unchanged. policy_loader.py, coverage_gate.py, and validate_invariants.py are deliberately excluded from that consolidation: docs/specs/policy-single-source.md already decided validate_invariants.py (and the runpy-invoked per-stack scanners) stay standalone-stdlib, and coverage_gate.py alone has upward of eight individually caplog-pinned SystemExit messages a shared raiser would have to reproduce exactly to be a safe, no-behavior-change merge. Considered and rejected: relocating harness/node/.governance/ to repository root -- DEC-005 already rejected exactly this (a root allowed-remotes.txt) because `.governance/**` is a declared-dormant protected_paths pattern; harness/CONTRACT.md gains a paragraph citing DEC-005 explicitly instead of a physical move. Also added: a root `install` target (harness/shared/install_hooks.sh) so a clone using only the root Makefile still gets the pre-push hook; header comments on harness/node/.github/workflows/ci.yml and harness/jvm/.github/workflows/ci.yml identifying them as reference templates GitHub never executes; the version string unified to pyproject.toml's 2.1.9 across README.md, docs/architecture/c4_architecture.md, and NEXT_STEPS.md, which had each drifted to a different value; harness/jvm/ explicitly labeled in README.md and harness/CONTRACT.md as an unadopted reference template with no live CI enforcement, rather than leaving a reader to infer that from INV-2/INV-3's prose alone -- no new CI wiring for that stack was in scope | governance-maintainers
2026-08-30 | DEC-014 | `make secrets`'s `gitleaks git` invocation (root, harness/node, and harness/jvm Makefiles, identically) scanned every ref present in the local clone, not just the checked-out branch's own history. Discovered when PR #35's `secret-scan` job failed despite a clean local `make secrets`: a completely unrelated branch pushed concurrently on this shared repository (`feature/governed-run-console`, touching neither PR #35's diff nor its ancestry) carries a real leaked key, and CI's fresh `fetch-depth: 0` clone picked it up while a long-lived local development clone, missing that branch, did not. Confirmed with a from-scratch `git clone` plus `gitleaks git . --log-opts="HEAD"` vs. without it: 141 commits scanned (the checked-out ref's own ancestry, clean) vs. 144 (every local ref, the unrelated leak). `--log-opts="HEAD"` restricts the walk to the current ref, matching INV-1's actual intent -- a PR's gate must be actionable by that PR's author -- rather than the prior behavior, which failed any PR for a secret on a branch it neither touches nor controls. All three `secrets` targets now pass `--log-opts="HEAD"`. Accepted consequence: a secret on a branch that isn't part of the ref under test is no longer caught by that ref's own gate; it remains caught the moment that branch's own PR is opened and scanned under this same, now-correctly-scoped rule | governance-maintainers
2026-08-30 | DEC-015 | GitHub Copilot's automated review of PR #35 (the DEC-013/DEC-014 tech-debt batch) found six real defects the batch's own local verification missed, all fixed in the same PR. `governance_json.read_json_object`'s `except OSError` did not catch `UnicodeDecodeError` -- a `ValueError` subclass raised by `Path.read_text()` before `json.loads()` ever runs -- so a badly-encoded policy file crashed uncaught instead of classifying as "malformed"; it now has its own except clause, matching check_dedup.py's pre-refactor behavior of treating a decode failure as corruption. `governance/broker.py`'s `_load_json` collapsed every failure into a plain `ValueError`, contradicting R-DH-5 ("each [caller] MUST keep raising its own existing exception type"): the pre-refactor function let `Path.read_text`/`json.loads` raise their natural `FileNotFoundError`/`OSError`/`JSONDecodeError`, and only the not-a-dict case was an explicit `ValueError`; `_load_json` now maps `read_json_object`'s `.error` back to those three type families explicitly, with new direct unit tests (`test_governance_broker.py::TestLoadJsonPreservesItsPreRefactorExceptionTypes`) since the only caller's broad `except Exception` had made the type distinction invisible at every existing test's altitude. `make pre-pr`'s prerequisite list never included `secrets`, contradicting README.md's claim that `pre-pr` runs both scanners; `secrets` joins its prerequisites (`ci`/`ci-python` deliberately still exclude it, unchanged, since interpreter-independent gates repeating per matrix leg is the exact anti-pattern that keeps `secrets` and `audit` as dedicated jobs in the first place). `pip-audit` was installed unpinned, in both the Makefile and CI, unlike gitleaks/osv-scanner; `PIP_AUDIT_VERSION` (2.10.1) now pins it in the root Makefile, matching `GITLEAKS_VERSION`/`OSV_VERSION`, and CI installs through `make audit-install-python` rather than an inline `pip install` that could drift from it. The `audit` job's `pip-audit` invocation resolved dependency markers and transitive constraints only under Python 3.11, even though the project supports and tests 3.9/3.10/3.12 -- a vulnerability specific to a dependency version selected only under another interpreter would never surface; `audit` splits into `audit-python` (interpreter-scoped scan) and `audit` (that plus the Node delegation, unchanged locally), and a new `audit-matrix` CI job runs `audit-python` across the same three interpreters as `build`, keeping the one-time Node delegation on the original `audit` job alone. `test_ci_gate_coverage.py`'s audit mapping proved only that some workflow contains a `make audit` command, not that nothing could guard it behind a conditional; a new `test_audit_gate_is_invoked_unconditionally` mirrors the existing secret-scan liveness check (no `if:` anywhere in the invoking job's block). Also fixed: GOVERNANCE_SKILL.md's DEC-013 summary said the two `__init__.py` files "are restored" and, later in the same paragraph, that adding them "was tried and reverted" -- a stale sentence left over from an earlier draft of the entry, contradicting both the authoritative decision-log.md text and the files' actual presence; removed | governance-maintainers
2026-08-30 | DEC-016 | DEC-015's `PIP_AUDIT_VERSION` pin (2.10.1) broke the `audit-matrix` job's 3.9 leg on its first real CI run: `pip-audit` 2.10.0 raised its own floor to `Requires-Python >=3.10`, so `pip install pip-audit==2.10.1` has no installable candidate under 3.9 and `make audit-install-python` failed closed with a dependency-resolution error rather than silently degrading. `dependency-audit (3.10)` and `dependency-audit (3.12)` both passed clean on the same commit, confirming the matrix job itself works as designed -- this was a version-pin defect, not a design defect. Capped at 2.9.0, the newest release that still declares `Requires-Python >=3.9`, matching this project's own floor exactly. Discovered from the actual CI run this PR's own `audit-matrix` job added, immediately validating the job's purpose: a single-interpreter local check (this development environment runs Python 3.11) could not have caught a 3.9-specific installability failure | governance-maintainers
2026-08-30 | DEC-017 | The `audit-matrix` job's first real run after DEC-016's fix found 7 known vulnerabilities the moment its 3.9 leg could actually execute `pip-audit`: `starlette` 0.49.3 (PYSEC-2026-161/248/249/2280/2281), `click` 8.1.8 (PYSEC-2026-2132), and `python-dotenv` 1.2.1 (PYSEC-2026-2270), all resolved only under Python 3.9 -- the `audit` job's single-interpreter 3.11 scan and `audit-matrix`'s 3.10/3.12 legs all found nothing, confirming DEC-015's premise (`pip-audit`'s resolution is interpreter-specific) with a real example on its very first run. Checked every currently-published fix directly against PyPI metadata: `starlette` >=1.0.1 (every 1.x release up to 1.6.0), `click` 8.3.3 and every later 8.3.x/8.4.x/8.5.x release, and `python-dotenv` >=1.2.2 all declare `Requires-Python >=3.10`; `python-dotenv` 1.2.1 -- the vulnerable version actually resolved -- is the newest release still declaring `>=3.9`. No `requirements.txt` pin can therefore make this leg both install under 3.9 and be clean under 3.9 at the same time: the project's Python 3.9 floor (flagged, per the user's explicit choice on this PR's originating plan, as a backlog item only) is now carrying unpatchable known CVEs in the API server's own runtime dependency stack, not merely an EOL-interpreter housekeeping concern. The 3.9 leg of `audit-matrix` is marked `continue-on-error` (`dependency-hygiene.md` C-DH-3/AC-8) rather than left blocking indefinitely on a gap this PR cannot close without bumping that floor; the finding is not silenced -- it remains in the step's own output on every run, is recorded here, and is added as new evidence to the tracking issue for the deferred floor bump. 3.10 and 3.12 stay fully enforced, with no `continue-on-error` of any kind | governance-maintainers
2026-08-30 | DEC-018 | `NEXT_STEPS.md`'s branch-ruleset paragraph listed 5 required status checks (`build (3.9)`, `build (3.10)`, `build (3.12)`, `build-full`, `secret-scan`) as "taken from a real run" -- true when written, but the list was never updated after DEC-013/DEC-016/DEC-017 added the `audit`/`audit-matrix` jobs, which report as `dependency-audit` and `dependency-audit (3.9/3.10/3.12)`. A ruleset configured from the stale list today would leave the entire dependency-audit gate (including the two CVE-bearing legs DEC-017 documents) permanently unrequired, with a green-looking ruleset masking it. Found while peer-reviewing an external analysis of this repository that flagged `main` having no branch ruleset as the highest-priority gap; verified directly against `.github/workflows/python-package.yml` rather than against the doc's own prior claim. The list is corrected to the 9 names GitHub actually reports, and `test_ci_gate_coverage.py` (`TestRequiredStatusChecksListIsAccurate`) now parses both the workflow file and the doc's list and fails closed on any divergence -- proved by temporarily renaming a job (doc-omits-check case) and by temporarily dropping a name from the doc (doc-lists-stale-check case), both confirmed to fail before being reverted. Applying the ruleset itself is a repository-settings change outside this PR's diff; this closes the mechanical drift that made the last three attempts at documenting the list wrong within days of being written | governance-maintainers
2026-08-31 | DEC-019 | A tech-debt audit flagged `digest(path) -> hashlib.sha256(path.read_bytes()).hexdigest()` as byte-identical triplicated code across `harness/control-plane/verify_repository.py`, `publish_policy_artifact.py`, and `regenerate_bundle_digests.py`. Confirmed intentional, not an oversight to dedupe: every control-plane script deliberately imports zero symbols from `harness.shared` (`harness/docs/ROOT_OF_TRUST.md`'s mandate that control-plane verifiers must not trust the governed project's own copy of anything an agent in that project could have modified). Deduplicating via a shared `harness.shared` helper would reintroduce exactly the dependency this design excludes; a control-plane-local shared helper would still be a fourth file to keep in sync and buys little at 2 lines each. Left triplicated by design; recorded here so a future audit does not re-flag it as undiscovered debt | governance-maintainers
2026-08-31 | DEC-020 | `harness/shared/`'s ~37 top-level files mix gate scripts (`check_*.py`, `validate_*.py`), tool plumbing (`tool_*.py`), and core orchestration with no subdirectory boundaries, unlike `governance/`/`langgraph/`, which are already split out. A full migration was considered and rejected for this pass: `protected_paths` has ~15 individual literal `harness/shared/{check,validate}_*.py` entries that would all need updating, `check_dedup.py`'s shim-classification logic enforces per-stack mirrors delegate to the exact current shared path, and the migration would add shim/indirection surface in the same audit that flagged reducing shim surface as a goal. Adopted instead: new gate-like modules land under `harness/shared/gates/` going forward; all 11 existing `check_*.py`/`validate_*.py` files stay exactly where they are. Zero shim churn, zero protected-path edits, zero `check_dedup.py` impact -- a convention for new code, not a migration of old code | governance-maintainers
2026-08-31 | DEC-021 | A request to "improve hooks/loops" considered wiring a coverage-regression check into `.mango/hooks/pre_completion_checklist.sh` (the Stop hook) and rejected it: that hook is declared in `.mango/settings.json`, which DEC-003 already established Claude Code does not read -- `.claude/settings.json` mirrors only the live `SessionStart` hook, by a human decision recorded as "its own reviewed change, never a side effect." Wiring new logic into a dormant hook would look wired while doing nothing, the exact failure class this repository's own liveness tests exist to catch elsewhere. Implemented instead through an already-live channel: `coverage-python` joins `.github/workflows/scheduled-drift.yml`'s nightly `main-drift` loop (`digest-regen`, `check-dedup`, `validate`), open-an-issue-on-failure, never blocking a PR -- matching that workflow's own stated design principle (drift no single PR's CI can catch). Justified specifically because `main` has no branch ruleset yet (DEC-018's still-open highest-value item): per-PR CI is advisory, not required, so a PR that dropped coverage below the policy floor could merge unnoticed until this catches it on `main` itself. Purely Python; the job needed no new Node/pnpm setup | governance-maintainers
2026-08-31 | DEC-022 | A second-round tech-debt audit (same PR) found two items shaped like this log's recurring "unlinked literal duplicating a policy value" pattern (Pattern 1: COV_MIN=80; the size_budget_lines/check_dedup/check_py_compat trio; DEC-005; DEC-006; DEC-009; the langgraph GraphPolicy fail-open earlier in this same PR) and evaluated each on its own facts rather than mechanically re-applying the same fix. `harness/shared/governance/verification.py`'s `VerificationRunner.__init__(..., timeout: int = 300)` duplicates `orchestrator.api_timeout_sec`'s policy value, but this module's own docstring states a stronger, deliberate contract than the already-fixed `process_backend.py` case: "the broker, the command and the timeout are all injected. Nothing here reads the environment or the filesystem at import time." Sourcing the default from policy would violate that documented import-time-purity guarantee (`policy_loader.orchestrator_defaults()` reads `governance-policy.json`), and the one production call site (`mango_mas_orchestrator.py`) already injects the real policy value explicitly -- the literal is confirmed unreachable in production; only ~10 `test_verdict.py` constructions omit it, which is ordinary test terseness, not a drift risk. Left as-is; recorded so a future audit does not re-flag it without this reasoning. Separately, `harness/shared/langgraph/decorators.py`'s `@with_authority`/`@budgeted` were found fully implemented and unit-tested in isolation but never applied to any of the 10 real node functions in `nodes.py` (only synthetic dummy functions in their own tests) -- contradicting `NEXT_STEPS.md`'s checked-off claim that they were "enforced... at node boundaries," now corrected to unchecked with the accurate state. Both decorators also fail *open* on a lookup error, the opposite of every other governance control in this repository, moot only while unwired. Wiring them is deliberately deferred rather than attempted in this hygiene pass: doing so correctly means fixing the fail-open behavior in the same change (this pattern's own lesson), which is a behavioral change to live-shaped node code, not a default-sourcing fix, and belongs in its own spec and review | governance-maintainers
2026-09-01 | DEC-023 | PR #55 added `mcp>=1.0.0,<3.0` to `requirements.txt` unconditionally, breaking CI two ways: Python 3.9 (no published `mcp` release supports below 3.10) and mypy (`mcp_server.py` called `types.Tool(..., inputSchema=...)`, but the field is `input_schema`). Inspecting `mcp`'s actual released source across versions rather than guessing from one mypy error: the package restructured `mcp.types` around a separate `mcp_types` dependency at 2.0.0, and `inputSchema` is the *only* accepted spelling below that -- `Tool`'s 1.x model has no `input_schema` field or alias, and `model_config=ConfigDict(extra="allow")` means passing it silently drops as an unmatched extra field, failing validation on the still-missing required `inputSchema`. A prior, now-superseded PR review comment argued the opposite rename was needed, citing `mcp==1.0.0` specifically -- correct for that exact version, but not for what this range actually resolves. Fixed by tightening the floor to `mcp>=2.0.0,<3.0; python_version >= "3.10"` and renaming the call site, rather than the narrower fix of merely pinning to whatever CI happens to resolve today. A second option was considered and rejected for now: moving `mcp` into `[project.optional-dependencies]` alongside `langgraph`, which is the architecturally-consistent choice -- `mcp` has the identical "gate on installed, not on Python version" shape `langgraph`'s own module docstring documents -- but no CI job installs any extras group today, so doing the same to `mcp` would permanently remove mypy's ability to catch this exact class of bug on every leg, the same gap already silently accepted for `langgraph`. Left in `requirements.txt` so mypy keeps checking it; revisit if this repo ever wires an extras-group install into CI, at which point `mcp` and `langgraph` should move together, not just one. Also added `test_dependency_lockstep.py`: `requirements.txt` and `pyproject.toml`'s `[project.dependencies]` had no mechanical check that they declare the same packages, which is exactly how this PR's `mcp` addition landed in one file and not the other, missed across four rounds of human review | governance-maintainers
2026-09-02 | DEC-024 | PR #60 merged into `main` with all three CI runs on its head (323-325) failed and a merge commit (`9d38670`) claiming "`make ci` ... passed" and "mypy ... clean"; the pushed code had reverted the DEC-023 `input_schema` rename, added a langgraph-dependent regression test no CI leg could satisfy, left a test patching a module path the decomposition had moved, and added ignore rules for a directory nothing creates. Nothing could have stopped the merge because `main` carried no ruleset (DEC-018's open item). Two controls adopted: (1) a ruleset export is committed at `.github/rulesets/main.json` requiring the nine status checks `test_ci_gate_coverage.py::TestRequiredStatusChecksListIsAccurate` derives from `python-package.yml` (strict, up-to-date-branch policy) plus one code-owner review, with `test_workflow_contracts.py` pinning the export's contexts to the derived names so the two cannot drift; applying it is a repository-settings action the owner performs by importing the file. (2) A stated rule in `CLAUDE.md`, `CONTRIBUTING.md` and the PR template: a verification claim in prose is not evidence, only the check runs on the pushed head are, and the `make ci`/`make lint-cold` tails are pasted rather than summarised. Considered and rejected: a CI job that fails when the PR body's Validation section is empty -- it is satisfiable by pasting anything and would train authors to paste, not to run; the ruleset is the control that would have blocked #60, the rule is the norm that explains why | governance-maintainers
2026-09-02 | DEC-025 | Constant triage (tech-debt-hardening-plan R-TDH-16; inventory and gate in `test_constant_triage.py`): every operational constant the audit found unlinked is now either a policy value or accepted here, and a third state is a test failure. Moved into policy: `process_backend.DEFAULT_MAX_OUTPUT_BYTES` becomes `orchestrator.max_output_bytes` (65536) read through `policy_loader.orchestrator_defaults()` -- a captured-output ceiling is a containment control, the same reasoning that moved `DEFAULT_TIMEOUT_SEC` earlier. Equality-pinned rather than re-sourced: `retry_policy.DEFAULT_MAX_RETRIES` must equal `nemotron.max_retries` (the module is stdlib-only by design and takes its overrides from the resolved mapping the bridge passes in). Accepted as true constants: `retry_policy.DEFAULT_BASE_SEC`, `DEFAULT_MAX_SEC` and `DEFAULT_JITTER_RATIO` (the bridge's backoff shape, overridable per call through the NEMOTRON_* mapping; a governance threshold would be `max_retries`, which is already policy); `shadow_planner.DEFAULT_SHADOW_TIMEOUT_SEC` (an operator env knob, `MANGO_SHADOW_TIMEOUT_SEC`, documented in .env.example and capped at the orchestrator API timeout, so it can never exceed a policy value); `cognitive_signal.MAX_SIGNAL_BYTES` and `MAX_SINK_BYTES` (protocol ceilings of the observation channel fixed by docs/specs/mangomas-integration-core.md R-MMI-3; changing them is a spec change, not a tuning). Accepted with a recorded divergence: the Node client's resilience defaults -- `harness/node/src/ai/nemotron/circuit-breaker.ts` (`failureThreshold` 5, `resetTimeoutMs` 10000, `halfOpenSuccessThreshold` 2) and `harness/node/src/ai/nemotron/nemotron-client.ts` (`baseBackoffMs` 500, `maxBackoffMs` 5000) -- are constructor-overridable client-local defaults with no Python counterpart of the same shape (Python backs off in seconds with proportional jitter), so there is nothing for them to drift from; a shared `resilience` policy block would be the way to unify them and is left as an open question rather than invented here. The policy-sourced Nemotron request values (`timeout_ms`, `max_retries`, `temperature`, `max_tokens`) are handled separately by R-TDH-13 | governance-maintainers
2026-09-02 | DEC-026 | Python skip accounting (INV-2 parity; tech-debt-hardening-plan R-TDH-19). Node proved "no unapproved skip" from Vitest's JSON; Python had nothing, and 36 langgraph cases plus four empty-parametrize skips ran unaccounted on every CI leg. Now: harness/shared/tests/conftest.py records every skip (`unique_id\tdisplay\treason`, via `_skip_events.py`) to a TSV that `make verify-zero-skips-python` -- in `ci` and `ci-python` -- feeds to the existing `verify_zero_skips.py --junit-events` gate, so no new format and no new dependency. The four empty-parametrize sites (`test_ci_gate_coverage.py`, `test_import_purity.py` x2, `test_test_quality.py`) became loops: an empty waiver registry is the healthy state and must not read as a skip. The gate gains an optional `unique_id_glob` for JUnit-framework waivers, matched with fnmatch against the whole nodeid, because the langgraph condition covers ~40 parametrised nodeids across three files and one row per nodeid would be a registry nobody maintains; a glob waiver still requires the decision id inside the skip reason, so a new skip under a waived path without `(DEC-026)` in its reason is unapproved. Waivers live in harness/shared/tests/skip-waivers.json rather than a root `.governance/` (kept dormant by DEC-005; plan open question 6 resolved this way). Waived conditions: NVIDIA_API_KEY absent (live suites), langgraph absent (a local run only; CI deselects on 3.9 and installs elsewhere), non-POSIX platform (bash hooks, chmod/symlink cases), mcp absent (3.9 only). Left unwaived on purpose: `test_makefile_contracts` (ci-python missing), `test_lint_config_liveness` (no per-file-ignores), `test_invariant_liveness` (dormant invariant) and `test_import_purity` (declared side effects) -- each fires only when a registry that should be empty is not, which is a signal to investigate, not to waive | governance-maintainers
2026-09-02 | DEC-027 | `autonomous_healing.py` and `lats_optimizer.py` are parked under `harness/shared/experimental/` (tech-debt-hardening-plan R-TDH-18; plan open question 3 resolved as "park"). Both shipped in v2.3.0 as headline features and have never been reachable from a runtime path: no orchestrator, node, CLI or API imports them, `synthesis.lats_enabled` is `false` with no reader, and INV-15 keeps LATS off until an ablation gate exists. The modules are byte-identical after the move, still policy-sourced (`orchestrator.max_healing_retries`, `lats.*`), still fully tested (`test_autonomous_healing.py`, `test_lats_optimizer.py`, `make test-lats`, the LATS regression in `test_e2e_nemotron_triage_regression.py`) and still measured by the coverage gate; what changes is that importing them is now visibly opting into an experiment, and the README, harness/README and C4 document say so. The old import paths are shims that re-export the public names and emit a `DeprecationWarning` for one minor release (C-TDH-2). Wiring either capability into a runtime path is its own spec, gated on `synthesis.lats_enabled` for LATS. In the same change the `MangoMASOrchestrator` facade loses the three private pass-throughs that existed only for tests (`_tool_handlers`, `_run_hook`, `_dispatch_tool_calls`); their tests now address `dispatcher` and `hook_runner` directly. `execute_sequential_thinking_loop` is not one of them: `orchestrator-tool-registry.md` R-ORCH-4 pins it as public surface and `verdict-propagation.md` R-VP-11 requires its prose return, so it stays | governance-maintainers
2026-09-02 | DEC-028 | The Python 3.9 floor (`requires-python >=3.9`) stays for now, and two consequences of keeping it in the CI matrix are handled explicitly instead of by red CI or an ignore flag. (1) Per-file coverage on the 3.9 leg. langgraph declares Requires-Python >=3.10, so that leg deselects the `langgraph`-marked suites (tech-debt-hardening-plan R-TDH-4) and `harness/shared/langgraph/{decorators,graph,nodes}.py` measure at 20-40% lines there, failing `coverage.per_file` while every other leg holds them at floor; the number was measuring the interpreter, not the code. `governance-policy.json -> coverage.optional_extras` now declares each optional extra with `import_name`, `deselect_env` and `path_prefixes`. `coverage_gate.py` waives the per-file lines floor for files under those prefixes only when that env is `1` AND `importlib.util.find_spec(import_name)` is None (the same predicate conftest.py uses to deselect), logs every waived file at WARNING with its measured value, keeps the aggregate `coverage.lines`/`coverage.branches` floors and every other file enforced, and fails closed on a malformed block. `conftest.py` reads the deselect variable's name from the same policy key through `policy_loader.coverage_optional_extras()`, so the deselect signal and the waiver have one source; `test_coverage_gate.py` pins the liveness (shipped policy names the extra, env and directory conftest and the workflow use). This is a structured, visible waiver, recorded here because INV-style gates do not get quiet exceptions. (2) pytest. PYSEC-2026-1845 is fixed in pytest 9.0.3, which requires Python >=3.10. requirements-dev.txt pins `pytest==9.0.3; python_version >= "3.10"` and `pytest==8.4.2; python_version < "3.10"`; the universal lock carries both with markers. The dependency-audit (3.9) job already runs continue-on-error (pre-existing, not introduced here), so the retained 8.4.2 pin shows there as a reported finding rather than being suppressed with `--ignore-vuln`. uv moves to 0.11.15 for its four advisories on every leg. Both waivers end the day the floor moves to 3.10, which is a product decision that needs its own spec (the JVM/Node adopters and the `check-compat` gate are the readers of that floor) | governance-maintainers
2026-09-02 | DEC-029 | Two structure decisions from tech-debt-hardening-plan Phase 4, recorded so the next audit does not re-derive them. (1) DEC-020 stands (R-TDH-20): `harness/shared/` is not regrouped into `core/`/`tooling/`/`runtime/`. The plan's revision-1 proposal was withdrawn under peer review because the split is cyclic on the real import graph (`policy_loader` <-> gate scripts <-> `governance/`), and because `protected_paths`, `check_dedup.py`'s shim classification and the 20 per-stack delegating scripts all name the flat paths. New gate-like modules land under `harness/shared/gates/` when one is needed; any move requires a superseding `DEC-` entry and an import-acyclicity test landed first. (2) The `sys.path` bootstraps stay as they are (R-TDH-21, AC-21's accepting-entry branch). `git grep -c "sys.path.insert" -- harness/shared harness/node/scripts harness/jvm/scripts` reports 31 sites in four styles: the shared entrypoints' `try: import harness.shared... except ImportError: sys.path.insert(0, <repo root>)` (the DEC-019 shape), the unconditional `parents[2]` in `validate_plan.py`/`validate_specs.py` (inside the same `except ImportError`), the ten per-stack `runpy` delegators, and the ten per-stack walk-up imports. Deduplicating them into a helper is impossible by construction: the bootstrap exists because `harness.shared` is not yet importable when a script runs as `python harness/shared/x.py` or from `harness/<stack>/scripts/`, so a helper module would itself need the bootstrap it replaces; the only real dedupe is a CLI-contract change (`python -m harness.shared.x`) for every adopter Makefile and hook, which is a product decision, not hygiene. The per-stack scripts are additionally root-of-trust artefacts (DEC-004: listed in `harness/<stack>/.governance/policy.json`, digested into the policy bundle), so rewriting twenty of them to save three lines each churns every digest for no behaviour change. `check_dedup.py` already bounds each per-stack shim to the shim-budget line count, which is the guard that matters. Revisit when the 3.9 floor moves and the entrypoint contract is reconsidered together (DEC-028) | governance-maintainers
2026-09-02 | DEC-030 | Session-wide pytest hooks belong at the rootdir. DEC-026 put the Python skip-evidence hooks (`pytest_runtest_logreport` collecting every skip, `pytest_sessionfinish` writing the TSV that `make verify-zero-skips-python` reads) and the R-TDH-4 langgraph deselection in `harness/shared/tests/conftest.py`. pytest scopes a conftest's per-item hooks to the directory that conftest sits in, so a skip under `harness/api_server/tests` produced no evidence row and the Python half of INV-2 silently covered only one of the two suites `make coverage-python` runs. Found by a probe while colocating `harness/control-plane/tests/` (tech-debt-hardening-plan R-TDH-26): two skipping tests in two sibling directories, zero rows written. The hooks now live in a repository-root `conftest.py` that delegates to `harness/shared/tests/_session_hooks.py` (a plain module, unit-testable); the shared conftest keeps re-exporting `LANGGRAPH_DESELECT_ENV`/`LANGGRAPH_MARKER` for the tests that pin the wiring, and registers no session hook, because a second registration would record every shared-suite skip twice. `test_session_hooks.py` proves the fix the way the gap was found: a `pytester` run of the real root conftest over two sibling suites yields both rows with rootdir-relative ids, and a placement test fails if any `harness/*/tests/conftest.py` defines one of the four hooks again. Considered and rejected: loading the hooks as a `-p` plugin from `addopts` (depends on cwd being importable before `pythonpath` applies, so a bare `pytest` would differ from `python -m pytest`) and re-importing the hook functions into each directory conftest (double registration). The waiver registry needed no change: the api_server suites currently produce no skips, so nothing was hidden, only unguarded | governance-maintainers
2026-09-02 | DEC-031 | Dependabot PRs #38-#46 are closed as superseded (tech-debt-hardening-plan R-TDH-10 / AC-10). Each proposed a single-package bump against the pre-lock layout; the universal `requirements-lock.txt` and the Phase 1 toolchain bump carry every one of them at or above the proposed version: #38 pytest-mock 3.15.1, #39 ruff 0.16.4 (branch: 0.16.5), #42 tomli 2.4.1, #43 knip 6.32.3, #44 eslint 10.9.1, #45 typescript-eslint 8.68.0, #46 @types/node 26.4.0, #41 pydantic >=2.13.5 (requirements.txt already declares it). #40 is closed for a different reason and not adopted: raising the fastapi floor to >=0.141.1 would break the Python 3.9 leg, because fastapi 0.141 requires 3.10; the lock resolves 0.141.1 on 3.10+ and 0.128.8 on 3.9, which is the correct shape until the floor moves (DEC-028). Merging any of the nine on top of the lock would either be a no-op or reintroduce a per-package pin the lock supersedes. Closing rather than leaving them open keeps the queue honest: an open bot PR reads as pending work. Dependabot proposes the next releases against the new baseline once #61 merges; the weekly `lock-upgrade-check` job (R-TDH-9) is the Python-side signal from then on | governance-maintainers
2026-09-02 | DEC-032 | Post-implementation review of the tech-debt hardening branch found four gates that could report PASS on absent or wrong evidence — the exact failure class DEC-024 was written about — plus one reproducible order-dependent test. All are fixed in the same change. (1) The Python zero-skip evidence hook read only `pytest_runtest_logreport`, so a module-level `pytest.importorskip` — which pytest reports as a `CollectReport`, never a `TestReport` — left no row and `make verify-zero-skips-python` printed `passed` while an entire module, sometimes a whole directory via its conftest, had vanished. Four live sites skip this way, including `test_egress_floor.py`, whose silent disappearance would remove the proof that the egress floor is armed. This is DEC-030's failure shape one layer up: there the hooks saw one of three suites, here one of two report types. Added `_skip_events.collect_skip_event` and a `pytest_collectreport` hook, with a `pytester` reproduction. (2) `coverage_gate._waiving_extra` matched with a raw `str.startswith`, so a policy prefix written without its trailing slash (`harness/shared/langgraph`) waived every sibling whose name merely began the same way (`langgraph_helpers.py`, `langgraphX.py`); the only policy check asserts the prefix names a real directory, which the slashless form does. Now matched on whole path segments. (3) `check_per_file` reported `[PASS] … 0 file(s)` when the waiver set covered everything — one policy edit (`path_prefixes: ["harness/"]`) disabled per-file enforcement entirely while the gate still said PASS. Now fails closed on zero measured files, consistent with the module's own "absence of evidence is never a pass" contract. (4) `test_constant_triage.check_row`'s decision-linkage check evaluated `Path("harness.shared.retry_policy").name.split(".")[0]` — `"harness"`, a substring of essentially every log line — and with `or`/`and` precedence only the symbol was ever really checked, so a constant could cite a decision about a different or nonexistent module and stay green; R-TDH-16/AC-16's entire enforcement rested on that expression. Replaced with a shape-aware `_module_names` and negative tests. `TEST_SIZE_BUDGET_LINES`, the one constant this branch itself introduced without a triage row, is now pinned to `limits.test_size_budget_lines`. (5) `json_logging.configure_gate_logging(__name__)` under `runpy.run_path(..., run_name="__main__")` permanently reconfigured the process-global `__main__` logger; fourteen modules across the three suites run scripts that way, and `test_nemotron_bridge.py::test_running_the_bridge_as_a_script_dispatches_main` failed whenever `test_pretooluse_guard.py` collected first. The full suite was green only because alphabetical order happened to be favourable — reverse file order was red. An autouse fixture in the repository-root conftest restores the logger; production behaviour is untouched. Also fixed: `_session_hooks` indexed the extras dict unguarded at module scope, so an adopter fork trimming `coverage.optional_extras` got a bare `KeyError` during conftest import with no diagnostic; and `deselect_langgraph` — the whole R-TDH-4 mechanism — had no test at all and was invisible to the coverage gate because `harness/shared/tests/*` is omitted from measurement. Correcting the record: DEC-027 states the parked modules are "byte-identical after the move". `lats_optimizer.py` is; `autonomous_healing.py` is not — it carries the `BROKER_SUCCESS` substitution from the Phase 2 verdict-constants work, which touched it before the move. The move was still content-preserving in intent; the word "byte-identical" was wrong and is corrected here rather than by editing an existing entry | governance-maintainers
2026-09-03 | DEC-033 | The `pip` ecosystem is removed from `.github/dependabot.yml`, completing DEC-031 rather than restating it. DEC-031 closed PRs #38-#46 as superseded by the universal `requirements-lock.txt` and named the weekly `lock-upgrade-check` job the Python upgrade signal from then on -- but left the ecosystem enabled, so the decision and the configuration disagreed in the only way that has an effect: twelve bot PRs reopened on 2026-09-02 (#62-#73), including a `mypy` 1.11 -> 2.3 major bump no lock-driven process requested. Against a locked dependency set a per-package pin is either a no-op or a reintroduction of the per-package pinning the lock supersedes, so the queue would have kept refilling with PRs the decision log says to close. The `npm` and `github-actions` ecosystems stay: neither is covered by the Python lock, and R-TDH-10 wants the action majors moving. The reopened pip PRs are left for the maintainer to close -- closing another account's PRs is not this change's to make -- and the reason is recorded here so the next audit does not read them as pending work. Re-enabling the ecosystem requires superseding DEC-031, not editing the config | governance-maintainers
2026-09-03 | DEC-034 | `make lint-node` is wired into `make ci`, and the blocker on record was not the real one. DEC-013 and `docs/specs/ci-enforcement-gaps.md` recorded a `typescript` 7.0.2 / `typescript-eslint` 8.67.0 incompatibility. Measured against the installed workspace on 2026-09-03 with the current pins (`typescript` ~6.0.3, `typescript-eslint` 8.68.0): ESLint passes, Knip passes, and the failure is `prettier --check` on exactly one file, `harness/node/.governance/policy.json`, over whitespace inside the `coverage.optional_extras.path_prefixes` array DEC-028 added. That file is digest-pinned -- `docs/rca/e2e_origin_sync_triage_rca_v2.5.0.md` records Prettier's reformat breaking the root-of-trust SHA256 and the remediation being to restore the exact bytes -- so Prettier's preferred formatting and `validate_adoption.py` are mutually exclusive by construction and running `prettier --write` is what breaks the digest, not what fixes the lint. Resolved by adding `harness/node/.prettierignore` excluding the digest-pinned `.governance/` tree, which is the only move that lets both gates hold; `make lint-node` then exits 0 and `make validate` still reports `adoption: passed`. `lint-node` becomes a direct prerequisite of `ci` and deliberately not of `ci-python` or the shared `lint` target, because the secondary matrix legs install no pnpm. This brings R-TDH-23's policy-sourced ESLint `max-lines` rule into a CI job for the first time -- it was enforced nowhere. Considered and rejected: reformatting `policy.json` and re-pinning the digest (it would recur on every DEC-028-shaped policy edit, and re-pinning a root-of-trust digest to satisfy a formatter inverts which artefact is authoritative) | governance-maintainers
2026-09-03 | DEC-035 | `coverage_gate.py` splits along the seam its own defects drew, and two defects in the gate-truthfulness batch's own gates are fixed. The gate reached 470 lines against a 500-line `limits.size_budget_lines` -- one edit of headroom -- so the measurement-*scope* concern moves to `harness/shared/coverage_scope.py` (per-file floor, optional-extra waivers, source discovery, measured-set bound) while `coverage_gate.py` keeps the aggregate-*threshold* concern (load_thresholds, measure, check, per_file_enabled, main). The seam is not a line count: every defect ever found in this area has been a scope defect rather than a threshold one -- the `startswith` waiver prefix that covered siblings, the waiver set that covered everything and still printed `[PASS] 0 file(s)` (both DEC-032), and the `omit` entry that dropped a file from the floor while raising the aggregate (R-GT-3). Backwards compatible by construction: `coverage_gate` re-exports every moved symbol including the two private helpers existing tests reach (`_importable`, `_waiving_extra`), the standalone `python harness/shared/coverage_gate.py` path keeps its ImportError fallback, and both modules measure 100%. The split invalidated three test-side assumptions that no assertion would have caught, each fixed: `test_the_gates_own_directory_cannot_shadow_the_extra` patched `coverage_gate.__file__` while `_importable` now reads `coverage_scope.__file__`, so the DEC-032 regression had stopped exercising anything (production behaviour is identical -- both modules sit in `harness/shared/`, so "the script's own directory" resolves to the same path); and sixteen `caplog.at_level(..., logger=cg.logger.name)` sites stopped seeing records emitted under the new module's name, now anchored on the `harness.shared` parent so a future move cannot blind them again. Separately, two defects in the batch's own new gates, found by probing them rather than by any test: `declared_source_roots`'s table regex required a following `[`, so a `pyproject.toml` whose last table is `[tool.coverage.run]` matched nothing and the gate failed closed reporting "declares no table" -- accurate about the regex, false about the file, and unrunnable for an adopter fork shaped that way; and `check_secret_allowlist.declared_keeps` scanned the whole config, so a `# keep:` comment anywhere in `.gitleaks.toml` -- a header comment, prose after the block, a commented-out rule -- exempted an entry from the liveness check, which is the same "a control an unreviewed edit elsewhere can widen" shape the batch exists to close. Keeps are now scoped to the `[allowlist]` block. Both carry regression tests that fail against the pre-fix code | governance-maintainers
2026-09-03 | DEC-036 | `nemotron.top_p` becomes a policy key, read by both stacks. The Node client hard-coded `top_p: 0.7` in two duplicated request-body literals; the Python bridge omitted `top_p` from its payload entirely. So the two stacks sent different sampling parameters to the same endpoint, and had done since the client was written -- a divergence `test_wire_format_parity_with_typescript` exists to prevent but cannot detect, because that test asserts the request succeeds rather than comparing the two bodies field by field (its docstring enumerated the fields from the Python payload alone, which is why `top_p` was missing from it). Adding the key to `harness/shared/governance-policy.json` required `policy_loader.NemotronDefaults`/`nemotron_defaults()` (exact-equality pinned by `test_policy_consistency.py::test_policy_loader_nemotron_fallbacks_mirror_policy`), the Node `NemotronPolicy` contract, and regenerating `harness/control-plane/policy-artifact.json`, which digests the shared policy (`publish_policy_artifact.py build`, not `make digest-regen` -- the per-stack mirrors carry no `nemotron` block and their root-of-trust digests are untouched). Value 0.7 preserves the Node behaviour exactly; the intended behaviour change is on the Python side, which now sends `top_p` where it previously let the provider default apply. The two duplicated literals collapse into `buildChatRequestBody` in `harness/node/src/ai/nemotron/request-body.ts`, a pure function taking the policy as an argument so a test can prove the body follows the policy it is given rather than one that happens to agree with the shipped file. `SAMPLING_BOUNDS` stays out of policy deliberately: the clamp ranges are the provider's accepted input domain, and a policy that widened them would describe a request the endpoint rejects | governance-maintainers
2026-09-03 | DEC-037 | `retry.ts`'s `JITTER_CEILING_MS` (200) is accepted as a true constant, with its own entry because DEC-025 does not cover it. DEC-025 accepts the Node resilience defaults on the stated ground that they have "no Python counterpart of the same shape", and names five constants -- `failureThreshold`, `resetTimeoutMs`, `halfOpenSuccessThreshold`, `baseBackoffMs`, `maxBackoffMs` -- none of which is this one, and it names neither `JITTER_CEILING_MS` nor `retry.ts`. A triage row citing DEC-025 for it therefore fails `check_row`'s linkage test, which is the correct outcome: `NEXT_STEPS.md` NS-16 called this "a triage row", implying a table edit, when the decision it would cite did not exist. The reasoning DEC-025 applies still holds and is restated here rather than assumed: the Python side jitters proportionally (`retry_policy.DEFAULT_JITTER_RATIO`, a dimensionless 0.25 of the computed delay), the Node side adds an absolute millisecond ceiling, so the two are not the same knob in two languages and have nothing to drift from. A governance threshold here would be `max_retries`, which is already policy-sourced on both sides. Recorded alongside three rows DEC-025 already justified but the inventory had never registered (`resetTimeoutMs`, `halfOpenSuccessThreshold`, `maxBackoffMs`) -- the table is the inventory, so a constant absent from it was unlinked in practice however well the decision read | governance-maintainers
2026-09-03 | DEC-038 | The protected-path attestation table required by `harness/CONTRACT.md` is derived and verified in CI instead of transcribed by hand. `check_protected_paths` fails a PR touching a protected path unless `ALLOW_GITHUB_CHANGES=1`, which the workflow derives from the `infra-reviewed` label, and the contract requires the PR description to carry a per-file table so the reviewer applying that label knows what they are attesting to. Nothing derived that table: it was copied out of a CI log, and on PR #76 -- this repository's own governance PR, whose subject is gates that pass on absent evidence -- a comment claimed the table had grown to thirteen rows when the validator's set at that head was ten, verified by replaying `is_protected` over `git diff origin/main...339cd6d`. A table a reviewer trusts, overstating what it covers, is the DEC-024 failure class reproduced inside the control written to prevent it, and it is the more dangerous direction: an inflated count reads as thoroughness. `harness/shared/governance/attestation.py` imports `is_protected`, `git_modified_files` and `load_protected_patterns` from `validate_invariants` rather than reimplementing them, so the printed table and the enforced set cannot diverge -- asserted as symbol identity, not as agreement on today's tree, because `git_modified_files` acquired its `core.quotePath=false` handling exactly the way a second implementation would have missed. `--check` compares a written description against that set and fails closed three ways: no attestation section, a section with no table, or any row-to-path mismatch in either direction (a row naming a path the change does not touch invites the reviewer to attest to something absent, which is the same overstatement pointed the other way). Table parsing is scoped to the section under a heading matching `--section` (default: any heading naming an attestation) because a PR description legitimately carries other tables; a header row is identified by the separator beneath it rather than by position, so a table introduced by a paragraph parses correctly. The base branch is never a literal -- explicit flag, then `GITHUB_BASE_REF`, then the remote's published `origin/HEAD` -- so an adopter fork whose default branch is not `main` needs no edit. Wired as `make attestation` / `make attestation-check` and as a `build-full` step that runs on every pull request and is deliberately *not* gated on the label, since the reviewer must be able to read a verified table before deciding; the description reaches the script through a file written from an environment variable, never interpolated into a shell command. The module lives under `harness/shared/governance/` so the existing `harness/shared/governance/**` pattern protects it: a gate that anything unreviewed can weaken is the defect this branch exists to close, and that placement costs it one row in its own table | governance-maintainers
2026-09-03 | DEC-039 | Seven operational defaults are accepted as true constants, and the inventory that was supposed to hold them is made complete. `test_constant_triage.py`'s table calls itself the inventory, but every assertion ran in one direction: each listed row was checked for a valid policy or decision link, and nothing checked that every constant which exists is listed. The stated rule -- "a new constant needs a row here" -- therefore had no enforcement, and the way to defeat the inventory was simply not to write the row. Seven had: `agent_prompts.TASK_LOG_PREVIEW_CHARS`, `debug_dump.DUMP_DIR_MODE` and `debug_dump.MIN_ENV_CREDENTIAL_LENGTH`, `meta_tools.DEFAULT_LOCK_TIMEOUT_S`, `meta_tools.DEFAULT_LOCK_POLL_S` and `meta_tools.MIN_LOCK_POLL_S`, and `tool_dispatch.DEFAULT_HYPOTHESIS_CONFIDENCE`. This is the same unbounded-scope failure DEC-032 found in `check_per_file` and DEC-038 found in the attestation table: a gate that judges the set it was handed without asking whether that set is the set that exists. `TestTheInventoryIsComplete` discovers module-level numeric constants from the source with `ast` (parsed, not imported, so the gate does not run the import side effects of the modules it judges) across `harness/shared`, `harness/api_server` and `harness/control-plane`, and requires each to be triaged or to carry an `EXCLUDED` entry stating why it is a fact rather than a limit; exclusions must name a reason, must still be discovered (a stale one is standing permission for a future constant of that name), and must not outnumber the triaged rows. Discovery is numeric-only on purpose -- the shape of an operational limit is a number, and demanding a decision for every string identifier would bury the real rows until the table stopped being read, which is how an inventory dies of over-collection. The seven are true constants, each for a reason of its own rather than by category. `meta_tools`' three are client-local resilience defaults for the advisory file lock on the meta-tool store, the same category DEC-025 accepts: `MIN_LOCK_POLL_S` is a floor that stops a zero or negative caller interval becoming a busy-spin, so it is a safety bound rather than a tunable, and both the timeout and poll interval are already caller-overridable parameters whose defaults matter only when no caller expressed a preference -- a governance threshold would be a threshold no operator has cause to set. `debug_dump.DUMP_DIR_MODE` is 0o700 because a dump directory holds prompts and tool output and the previous code took the default `0o777 & ~umask`, leaving that history world-readable on a shared host; owner-only is a security posture, and a policy able to widen it would be a policy able to reintroduce the defect. `debug_dump.MIN_ENV_CREDENTIAL_LENGTH` is a correctness floor, not a tuning knob: `redact_text` replaces by substring, so accepting a one-character environment value as a credential would rewrite every occurrence of that character and destroy the history it exists to preserve. `tool_dispatch.DEFAULT_HYPOTHESIS_CONFIDENCE` is the neutral midpoint used when a model omits confidence from a `hypothesis_register` call -- a defined value for a missing field, where any other number would assert a belief the model did not express. `agent_prompts.TASK_LOG_PREVIEW_CHARS` bounds how much of a task string reaches a log line; it trades log volume against correlatability and touches no governed behaviour. Eight further discovered names are recorded as `EXCLUDED` rather than triaged because they are facts, not limits: the hex-width constants in `publish_policy_artifact` and `shadow_planner` (`POLICY_VERSION_HEX_LEN`, `SHA256_HEX_LEN`, `TASK_ID_HEX_LEN` -- SHA-256 is 64 hex characters whatever a policy says), `pretooluse_guard`'s `ALLOW_EXIT`/`BLOCK_EXIT` (defined by the Claude Code hook protocol, not by this repository), and `langgraph`'s `EXPECTED_NODE_COUNT`/`CHANNEL_COUNT` (structural facts about the compiled graph and the state schema that their own tests pin) | governance-maintainers
2026-09-03 | DEC-040 | The attestation check reads the pull request description from the API, not from `github.event.pull_request.body`, and `edited` joins the workflow's trigger types. Correcting DEC-038, which specified the payload field: that field is a snapshot taken when the run was queued, and the mechanism failed on its second real run in three separate ways. (1) Staleness: the description was corrected minutes after the push that queued the run, so the check judged text that no longer existed and reported a missing row for `.mango/skills/protected-path-attestation/SKILL.md` against a table that already had one. (2) Unclearability, which is the serious one: `edited` was not a trigger type and "Re-run failed jobs" replays the *original* event payload, so a stale description could never be cleared -- the only way past the gate would have been an otherwise pointless commit, and a gate whose only escape is a no-op commit trains people to make no-op commits. (3) Disclosure: passing the description through an environment variable printed the entire body into the step-setup group of the CI log, which is noise at best and, for a description that ever carried something sensitive, a leak into logs retained beyond the PR. The step now fetches `/repos/{owner}/{repo}/pulls/{number}` with the job token, writes `.body` to a file with a one-line Python reader, and passes only the path; the description still never reaches the shell as code. `set -euo pipefail` is explicit because the runner's default `bash -e` does not set pipefail, and a failed fetch would otherwise leave an empty file that the gate reports as a missing table -- a true failure for a false reason, which is the diagnosis-obscuring shape DEC-032 objected to. `pull-requests: read` is declared on `build-full` alone rather than workflow-wide, so the matrix and audit jobs keep `contents: read` and nothing more; a test asserts that scoping, and a second asserts the payload field does not return. `edited` fires on title edits too, which is the accepted cost of a description fix being able to re-run the check that judges it. Also recorded: the first version of the `edited` assertion searched the trigger section for the string and passed on the comment explaining why `edited` is there, so deleting the type left it green -- found by mutating the workflow rather than by reading the test, and now asserted against the parsed `types:` list | governance-maintainers
